
let username=prompt("Enter the Name")
let age=Number(prompt("Enter the age in Nos."))

const userInfo=(username,age)=>{
console.log(`Hello ${username} , You are ${age} years Old`)
}

userInfo(username,age)